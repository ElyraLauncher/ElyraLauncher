<?xml version="1.0" encoding="utf-8"?>
<!--
    Elyra compact search trigger pill.
    Fixed width (elyra_search_trigger_width = 240dp), centred above the page indicator zone.
    NOT a full-width Google bar — a compact branded pill that does not dominate the screen.
    Positioned in DragLayer by ElyraHomeWidgetsController.attachSearchTriggerToDragLayer().
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/elyra_search_trigger_root"
    android:layout_width="match_parent"
    android:layout_height="@dimen/elyra_search_trigger_height"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/elyra_search_trigger_bg"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:src="@drawable/elyra_ic_search"
        android:tint="@color/elyra_search_trigger_text"
        android:contentDescription="@null" />

    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:textColor="@color/elyra_search_trigger_text"
        android:textSize="13sp"
        android:text="@string/elyra_search_hint"
        android:singleLine="true"
        android:ellipsize="end" />

</LinearLayout>
